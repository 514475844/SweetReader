# 更新日志

本项目的所有重要变更都会记录在此文件中。

## [2.4] - 2026-09-20

### (B) 查重去重升级为「内容相似度」判定 + 自动勾选
### (E) 修复邀请码功能完全不可用（根因 500）
### (1) 首页删除无用的阅读状态筛选
### (2) 首页排序新增「按文件大小」
### (3) Logo 更大更清晰
### (4) 阅读页底部重构（移动端 + PC 同时修复）
### (5) 页脚改版：清晰 logo + 讨论群 + GitHub
- 删除纯文字品牌行「SweetReader / 本地书库 · 安静阅读」，改为**清晰矢量 logo ＋ 品牌名 ＋ 两个链接**。
- 已适配深色主题；移动端 ≤520px 纵向堆叠，链接触控高度 38px。
- 注：此前「取消所有外链」的产品决定，已由用户确认**保留这两个外链**。
### (1) 原 logo 回归（此前误用了自造的「SR」图标）
- 上一轮把 logo 换成了自造的蓝底白字「SR」矢量图，那不是原 logo，已废弃删除。
### (2) PC 端吸顶：页头常驻 + 消除左上空白
### (3) 筛选栏：去掉两个图标 + 视觉优化
- 移除筛选栏里的 🔤 与 📄 两个 emoji 图标。
- 视觉重做：筛选栏改圆角卡片（细描边 + 柔和阴影）；字母按钮 30px 方形圆角、格式按钮改胶囊；统一 hover / active（主色 + 微上浮 + 阴影）；搜索框与排序下拉同高对齐；移动端断点同步收敛尺寸。
### (4) 「继续阅读」卡片文字溢出
### (5) 排序只作用于「当前已显示」的书籍
- 需求：排序只重排当前已加载的 50 / 100 本，而不是整库重排；只有字母与格式筛选才走全库查询。
### (6) 重复书籍删除 = 真删磁盘文件 + 阅读器数据
- 前端「删除选中」改为**一次批量请求**（原来 N 个并发单删），确认框明确列出将删除磁盘文件 / 封面 / 阅读进度与书签，完成后弹窗回报实际删除数量与失败 id。
### (1) 阅读页去掉「统计」超链接
### (2) 修复「背景纹理」完全没有效果（是 BUG，不是没做）
### (3) 操作日志倒序
### (4) 用户管理：显示在线 + 搜索 / 筛选
- 筛选栏：关键词（用户名 / 邮箱）、角色（全部 / 管理员 / 子管理员 / 普通用户）、状态（全部 / 🟢 在线 / ✅ 正常 / ⛔ 禁用）+ 重置。
- 列表新增「在线」列（绿点 + 徽标）与「最近活跃」列（显示最后活跃时间，从未登录显示「从未登录」）。
- 移动端适配：筛选输入与下拉在窄屏换行铺满。
### 1. 插件文档进仓库（用户点名）
### 2. 禅模式与全屏合并 + 移动端禅模式显示不全
- 删除「全屏」按钮，禅模式成为唯一入口：进入即请求全屏；退出（按钮 / Esc / 系统退出全屏）同步退出全屏（监听 fullscreenchange 对齐状态）。
- 禅模式不再隐藏「读完推荐」区。
### 3. 读完一本书后显示相关推荐（原因是从未生效）
- 现在：正文末尾新增推荐区（读到末尾露出 + Toast）；TXT 按滚动进度 ≥99.5% 判定读完，EPUB/PDF 直接置于末尾展示；卡片为自适应网格（手机两列 / 超窄屏单列），配色随页面底色自适应。
### 4. 插件管理上传入口
- 用户插件支持「删除插件」（内置与随包插件不可删，导入失败的 broken 项也可删）。
### 5. 阅读页翻页效果（真翻页）
- 原「翻页」只是按屏滚动。现改为 CSS 多列 + 横向定位：列宽 = 可视宽度，一列即一页；滑动 / 点击两侧 / ←→ / PageUp PageDown / 滚轮均可翻页，拖拽松手吸附整页。
- 翻页按钮由上下改为左右（‹ ›）贴两侧居中；翻页模式下取消底部大留白。默认仍为滚动，可在设置面板切换（本地记忆）。
### 6. 页头重设计
### 7. 乱码与损坏文件（修复 / 删除）—— 新管理页
### 8. 移动端逐项复核
- 新页面（乱码页、插件上传、页头搜索、翻页按钮、推荐卡片）全部带窄屏规则；13 个页面登录态回归全部 200。
### 2026-09-19 · 修复「导出书籍元数据」失效
- **结果**：导出正常，全量书籍元数据（含书名、作者、文件名、格式、体积、分类、标签、入库与最近阅读时间）可正常下载，中文在 Excel 中不乱码。
- **顺带**：新增一套自动化回归检查工具，每天发布前会先跑一遍全站页面与核心接口，**有问题当天就不发布**。
### 1. 筛选与排序明显变快
- 书籍列表接口由「一次性返回全部书籍」改为**分页返回**（默认 50 条，单次最多 200 条），并支持按格式与关键词筛选。
- 书籍查重：修复了一处「每比对一对书就重新线性扫描整个书库」的实现，查重耗时下降约四分之一。
### 2. 修复两个一直存在的筛选错误
- **修复：点击字母筛选后，显示的书目数量与按钮上的数量对不上。**
- **修复：在包含多级子分类的分类里筛选时，只显示直接子分类的书，更深层级的书被漏掉。**
### 3. 其它
- 格式筛选不再区分大小写，个别入库时格式名大小写不一致的书也能被正确筛选出来。
- 清理了一段没有任何页面引用的历史脚本文件。
### 1. 修掉三处「窄屏下整页被撑宽」的问题
- **乱码与损坏文件页**：分类路径标签在窄屏下不收缩，单个长标签就能把页面顶宽，
### 2. 移动端交互手感
- 管理页的宽表格统一改为**横向滑动查看**，不再挤压或撑破页面。
- 移动端按钮此前被压得偏小（实际可点高度约 20px），现已抬高到便于点按的尺寸，
- 触屏操作去掉了双击缩放带来的点击延迟。
### 3. 无障碍：尊重系统「减少动态效果」
### 1. 首页「扩展功能」面板可以收起，并记住你的选择
- 面板右上角新增「收起 / 展开」。收起后整个卡片区平滑折叠（180 毫秒，不做弹跳），
- 面板内新增两个快捷入口：**搜索**（一键跳到搜索框并聚焦）、**扫描**（管理员可见，重新扫描书库目录）。
### 2. 扩展卡片可以自己配置（每个账号各一份）
- 设置页新增「首页扩展」区块，逐张卡片开关，关闭的卡片不再出现在首页扩展面板里。
- 配置按账号保存，换设备登录同样生效；管理端的插件总开关优先级更高，普通用户只能让卡片变少、不能越过管理员启用。
- 被关掉的卡片仍保留在设置页清单中，随时可以重新打开。
### 3. 新增「书库总览」卡片
- 扩展面板新增一张总览卡：藏书总数、分类数、格式种数，以及**未读 / 在读 / 已读**三段分布条与数量。
- 数据由后端聚合查询直接统计（不搬运全量书目），大书库下也是毫秒级返回。
### 4. 新增阅读状态筛选
- 筛选栏新增「阅读状态」一行：未读 / 在读 / 已读，按钮上直接显示数量，再次点击同一项即可取消。
- 可与字母、格式、标签、搜索、分类筛选叠加使用。
### 1. 键盘快捷键基本补全（桌面端）
- 新增：**空格 / Shift+空格**翻页、**Home / End**跳到开头与结尾、**+ / −**调字号、
- 顺带修掉一个容易误触的问题：带 Ctrl / Cmd / Alt 的组合键不再落入单键逻辑
### 2. 新增「快捷键与手势」说明面板
- 阅读页工具栏新增「快捷键」入口，面板内按**翻页 / 显示 / 面板 / 触摸手势**四组列出全部操作方式，
- 打开与关闭都是 180 毫秒的轻淡入，按 Esc 或点空白处即可关闭。
### 3. 首次进入阅读页的操作指引
- 第一次打开某本书时，底部会出现一条轻量提示：左右滑动切章、双指捏合调字号、点两侧翻页、
- 只提示一次（浏览器记住），也可随时从工具栏重新打开。
### 4. 自动滚动 / 自动翻页
- 设置面板新增「自动滚动」开关与**慢 / 中 / 快**三档速度：滚动模式下平稳向下滚动，
- 右下角常驻一枚小状态条，点一下即可暂停；到结尾会自动停止并提示。
- 一旦你手动滚动、触摸或按键，自动滚动立即暂停，不会和手动操作打架。
### 5. 翻页按钮位置可自定义
- 「翻页按钮」新增四种选择：**两侧（默认）/ 仅左侧 / 仅右侧 / 隐藏**，选择立即生效并本地记住。
### 6. 修复：禅模式中切换主题导致界面错乱
- 修复了在禅模式下切换主题时，沉浸与禅模式状态被一起重置、工具栏重新出现的问题。
### 1. 正文文字颜色可自定义
- 设置面板新增「文字颜色」：跟随主题、暖黑、墨黑、墨蓝、深棕、护眼绿，也可以直接用取色器选任意颜色。
- 切换主题后你选的颜色依然保留；选「跟随主题」即恢复默认。
### 2. 主题色（强调色）可自定义
- 新增「强调色」：默认、樱粉、雾蓝、青绿、藕紫、琥珀，或自定义取色；
### 3. 高级用户自定义样式
- 设置面板底部新增自定义样式输入框，可写自己的 CSS（例如调整字间距），点「应用」立即生效，「清空」还原。
- 内容有长度上限并做了安全处理，写坏也不会影响页面本身。
### 4. 配色切换更顺滑
- 切换主题、页面背景色、文字颜色时加入 200 毫秒的柔和过渡；
- 若系统开启了「减少动态效果」，过渡会自动关闭。
### 5. EPUB 阅读：表格与代码块显示修复
- EPUB 里的**表格**现在有清晰边框、表头底色，窄屏可左右滑动查看，不再撑破版面；
- **代码块**使用等宽字体与浅色底、可横向滚动，行内代码同样有底色；引用块与图片也不再溢出。
### 6. 修复：EPUB 电子书一本都打不开（重要）
- 修复了一个底层资源损坏问题：EPUB 阅读器依赖的脚本文件内容被破坏，导致打开任何 EPUB 都停在
- 同时修复了书籍地址缺少格式后缀时，阅读器误判为「目录」而卡住的问题。
### 7. 修复：快速连续调整设置时，后改的值可能被覆盖
- 现在连续拖动滑块、连续切换选项只会在停止操作后保存一次最终结果，不会再出现「刚改的又被改回去」。
### 1. 修复：调完字号、行距、字体、对齐、亮度后可能「白调」
- 此前这几项改完并不会立刻保存，必须再点一下设置面板外面才写入；调完直接刷新或关掉页面就丢了。
### 2. 翻页模式新增页码显示
- 底部信息栏在**翻页模式**下会额外显示「第 x / y 页」，与章节进度并列；滚动模式不显示页码。
### 3. EPUB 阅读器新增「排版」面板
- 阅读器顶部新增「排版」按钮，展开可调 **字号（12–28 连续可调）/ 行距 / 亮度 / 字体**，
- 面板开合是 180 毫秒轻淡入；若系统开启了「减少动态效果」，动画会自动关闭。
### 4. EPUB 阅读器跟随你的主题
- 暗色 / 护眼主题下，EPUB 阅读界面的工具栏与正文区域一起换色，不会出现「暗色里突然一块白底」。
### 5. 阅读页设置面板对 EPUB 同样有效
- 在阅读页设置面板里调字号 / 行距 / 亮度 / 主题，现在会**直接作用到 EPUB 正文**（此前完全没有反应）。
- 面板打开时显示的就是你真实的偏好，不再停在默认值。
### 6. 修复：EPUB 阅读进度不保存（重要）
- EPUB 阅读器的进度上报逻辑此前没有接通：翻页后**进度条不动、进度也不会保存**，
### 1. 分类浏览页更好用（/categories）
- **树形层级看得清了**：分类树的每一层加了左侧引导线，鼠标悬停有高亮反馈。
- **展开状态会被记住**：展开哪几个分类，刷新页面或切换视图后仍然保持。
- **新增「全部展开 / 全部收起」按钮**：想看整棵树一次点开，想收起来一键收起。
### 2. 分类管理页可删除空分类（管理员）
- 分类管理页每行新增「删除空分类」按钮；**只对空分类可点**，其下还有书籍或子分类的行会自动置灰
### 3. 首页新增「标签」筛选
- 书架筛选栏新增「标签」一行：列出常用标签（带数量），**点一下即按该标签筛选，再点一次取消**；
- 标签较多时默认只显示前 12 个，可点「更多」展开全部；库内还没有标签时这一行自动隐藏，不占版面。
### 4. 修复：分类浏览页数量不显示
- 分类浏览页顶部的「分类数 / 藏书数」此前不会显示（脚本读了一个页面上并不存在的元素，
### 5. 修复：部分分类名「看不见的字符」与排序错乱
- 仅处理显示，**分类路径未做任何改动**，原有链接与归类完全正常。
### 1. 新增「整理工具」卡片（书籍管理页）
- **文件名智能整理**—— 扫描出「文件名叫得不规范」的书，给出「原名 → 建议名」对照与整理理由
- **查找分册（上下册 / 第N部 / 第N卷）**—— 把同一套书的上下册、第几部自动聚成一组并排好顺序，可整组合并成一本。
- **查找分章（第N章 / 编号结尾）**—— 同上，针对「书名-1、书名-2」这类分章文件。
### 2. 新增单本书「拆分」
- 书籍列表每行新增「拆分」按钮：可把一个纯文本（txt / md）**按章节标题切**，或**按长度等分切**
- 弹窗里先点「预览方案」，看清将生成哪几卷、每卷多少字、起始是哪一行，再点「确认拆分」。
- 拆分结果是**新增分卷，原书保留不动**；分卷自动继承原书的分类与格式，并直接入库可读。

## [2.4] - 2026-09-20

### (B) 查重去重升级为「内容相似度」判定 + 自动勾选
### (E) 修复邀请码功能完全不可用（根因 500）
### (1) 首页删除无用的阅读状态筛选
### (2) 首页排序新增「按文件大小」
### (3) Logo 更大更清晰
### (4) 阅读页底部重构（移动端 + PC 同时修复）
### (5) 页脚改版：清晰 logo + 讨论群 + GitHub
- 删除纯文字品牌行「SweetReader / 本地书库 · 安静阅读」，改为**清晰矢量 logo ＋ 品牌名 ＋ 两个链接**。
- 已适配深色主题；移动端 ≤520px 纵向堆叠，链接触控高度 38px。
- 注：此前「取消所有外链」的产品决定，已由用户确认**保留这两个外链**。
### (1) 原 logo 回归（此前误用了自造的「SR」图标）
- 上一轮把 logo 换成了自造的蓝底白字「SR」矢量图，那不是原 logo，已废弃删除。
### (2) PC 端吸顶：页头常驻 + 消除左上空白
### (3) 筛选栏：去掉两个图标 + 视觉优化
- 移除筛选栏里的 🔤 与 📄 两个 emoji 图标。
- 视觉重做：筛选栏改圆角卡片（细描边 + 柔和阴影）；字母按钮 30px 方形圆角、格式按钮改胶囊；统一 hover / active（主色 + 微上浮 + 阴影）；搜索框与排序下拉同高对齐；移动端断点同步收敛尺寸。
### (4) 「继续阅读」卡片文字溢出
### (5) 排序只作用于「当前已显示」的书籍
- 需求：排序只重排当前已加载的 50 / 100 本，而不是整库重排；只有字母与格式筛选才走全库查询。
### (6) 重复书籍删除 = 真删磁盘文件 + 阅读器数据
- 前端「删除选中」改为**一次批量请求**（原来 N 个并发单删），确认框明确列出将删除磁盘文件 / 封面 / 阅读进度与书签，完成后弹窗回报实际删除数量与失败 id。
### (1) 阅读页去掉「统计」超链接
### (2) 修复「背景纹理」完全没有效果（是 BUG，不是没做）
### (3) 操作日志倒序
### (4) 用户管理：显示在线 + 搜索 / 筛选
- 筛选栏：关键词（用户名 / 邮箱）、角色（全部 / 管理员 / 子管理员 / 普通用户）、状态（全部 / 🟢 在线 / ✅ 正常 / ⛔ 禁用）+ 重置。
- 列表新增「在线」列（绿点 + 徽标）与「最近活跃」列（显示最后活跃时间，从未登录显示「从未登录」）。
- 移动端适配：筛选输入与下拉在窄屏换行铺满。
### 1. 插件文档进仓库（用户点名）
### 2. 禅模式与全屏合并 + 移动端禅模式显示不全
- 删除「全屏」按钮，禅模式成为唯一入口：进入即请求全屏；退出（按钮 / Esc / 系统退出全屏）同步退出全屏（监听 fullscreenchange 对齐状态）。
- 禅模式不再隐藏「读完推荐」区。
### 3. 读完一本书后显示相关推荐（原因是从未生效）
- 现在：正文末尾新增推荐区（读到末尾露出 + Toast）；TXT 按滚动进度 ≥99.5% 判定读完，EPUB/PDF 直接置于末尾展示；卡片为自适应网格（手机两列 / 超窄屏单列），配色随页面底色自适应。
### 4. 插件管理上传入口
- 用户插件支持「删除插件」（内置与随包插件不可删，导入失败的 broken 项也可删）。
### 5. 阅读页翻页效果（真翻页）
- 原「翻页」只是按屏滚动。现改为 CSS 多列 + 横向定位：列宽 = 可视宽度，一列即一页；滑动 / 点击两侧 / ←→ / PageUp PageDown / 滚轮均可翻页，拖拽松手吸附整页。
- 翻页按钮由上下改为左右（‹ ›）贴两侧居中；翻页模式下取消底部大留白。默认仍为滚动，可在设置面板切换（本地记忆）。
### 6. 页头重设计
### 7. 乱码与损坏文件（修复 / 删除）—— 新管理页
### 8. 移动端逐项复核
- 新页面（乱码页、插件上传、页头搜索、翻页按钮、推荐卡片）全部带窄屏规则；13 个页面登录态回归全部 200。
### 2026-09-19 · 修复「导出书籍元数据」失效
- **结果**：导出正常，全量书籍元数据（含书名、作者、文件名、格式、体积、分类、标签、入库与最近阅读时间）可正常下载，中文在 Excel 中不乱码。
- **顺带**：新增一套自动化回归检查工具，每天发布前会先跑一遍全站页面与核心接口，**有问题当天就不发布**。
### 1. 筛选与排序明显变快
- 书籍列表接口由「一次性返回全部书籍」改为**分页返回**（默认 50 条，单次最多 200 条），并支持按格式与关键词筛选。
- 书籍查重：修复了一处「每比对一对书就重新线性扫描整个书库」的实现，查重耗时下降约四分之一。
### 2. 修复两个一直存在的筛选错误
- **修复：点击字母筛选后，显示的书目数量与按钮上的数量对不上。**
- **修复：在包含多级子分类的分类里筛选时，只显示直接子分类的书，更深层级的书被漏掉。**
### 3. 其它
- 格式筛选不再区分大小写，个别入库时格式名大小写不一致的书也能被正确筛选出来。
- 清理了一段没有任何页面引用的历史脚本文件。
### 1. 修掉三处「窄屏下整页被撑宽」的问题
- **乱码与损坏文件页**：分类路径标签在窄屏下不收缩，单个长标签就能把页面顶宽，
### 2. 移动端交互手感
- 管理页的宽表格统一改为**横向滑动查看**，不再挤压或撑破页面。
- 移动端按钮此前被压得偏小（实际可点高度约 20px），现已抬高到便于点按的尺寸，
- 触屏操作去掉了双击缩放带来的点击延迟。
### 3. 无障碍：尊重系统「减少动态效果」
### 1. 首页「扩展功能」面板可以收起，并记住你的选择
- 面板右上角新增「收起 / 展开」。收起后整个卡片区平滑折叠（180 毫秒，不做弹跳），
- 面板内新增两个快捷入口：**搜索**（一键跳到搜索框并聚焦）、**扫描**（管理员可见，重新扫描书库目录）。
### 2. 扩展卡片可以自己配置（每个账号各一份）
- 设置页新增「首页扩展」区块，逐张卡片开关，关闭的卡片不再出现在首页扩展面板里。
- 配置按账号保存，换设备登录同样生效；管理端的插件总开关优先级更高，普通用户只能让卡片变少、不能越过管理员启用。
- 被关掉的卡片仍保留在设置页清单中，随时可以重新打开。
### 3. 新增「书库总览」卡片
- 扩展面板新增一张总览卡：藏书总数、分类数、格式种数，以及**未读 / 在读 / 已读**三段分布条与数量。
- 数据由后端聚合查询直接统计（不搬运全量书目），大书库下也是毫秒级返回。
### 4. 新增阅读状态筛选
- 筛选栏新增「阅读状态」一行：未读 / 在读 / 已读，按钮上直接显示数量，再次点击同一项即可取消。
- 可与字母、格式、标签、搜索、分类筛选叠加使用。
### 1. 键盘快捷键基本补全（桌面端）
- 新增：**空格 / Shift+空格**翻页、**Home / End**跳到开头与结尾、**+ / −**调字号、
- 顺带修掉一个容易误触的问题：带 Ctrl / Cmd / Alt 的组合键不再落入单键逻辑
### 2. 新增「快捷键与手势」说明面板
- 阅读页工具栏新增「快捷键」入口，面板内按**翻页 / 显示 / 面板 / 触摸手势**四组列出全部操作方式，
- 打开与关闭都是 180 毫秒的轻淡入，按 Esc 或点空白处即可关闭。
### 3. 首次进入阅读页的操作指引
- 第一次打开某本书时，底部会出现一条轻量提示：左右滑动切章、双指捏合调字号、点两侧翻页、
- 只提示一次（浏览器记住），也可随时从工具栏重新打开。
### 4. 自动滚动 / 自动翻页
- 设置面板新增「自动滚动」开关与**慢 / 中 / 快**三档速度：滚动模式下平稳向下滚动，
- 右下角常驻一枚小状态条，点一下即可暂停；到结尾会自动停止并提示。
- 一旦你手动滚动、触摸或按键，自动滚动立即暂停，不会和手动操作打架。
### 5. 翻页按钮位置可自定义
- 「翻页按钮」新增四种选择：**两侧（默认）/ 仅左侧 / 仅右侧 / 隐藏**，选择立即生效并本地记住。
### 6. 修复：禅模式中切换主题导致界面错乱
- 修复了在禅模式下切换主题时，沉浸与禅模式状态被一起重置、工具栏重新出现的问题。
### 1. 正文文字颜色可自定义
- 设置面板新增「文字颜色」：跟随主题、暖黑、墨黑、墨蓝、深棕、护眼绿，也可以直接用取色器选任意颜色。
- 切换主题后你选的颜色依然保留；选「跟随主题」即恢复默认。
### 2. 主题色（强调色）可自定义
- 新增「强调色」：默认、樱粉、雾蓝、青绿、藕紫、琥珀，或自定义取色；
### 3. 高级用户自定义样式
- 设置面板底部新增自定义样式输入框，可写自己的 CSS（例如调整字间距），点「应用」立即生效，「清空」还原。
- 内容有长度上限并做了安全处理，写坏也不会影响页面本身。
### 4. 配色切换更顺滑
- 切换主题、页面背景色、文字颜色时加入 200 毫秒的柔和过渡；
- 若系统开启了「减少动态效果」，过渡会自动关闭。
### 5. EPUB 阅读：表格与代码块显示修复
- EPUB 里的**表格**现在有清晰边框、表头底色，窄屏可左右滑动查看，不再撑破版面；
- **代码块**使用等宽字体与浅色底、可横向滚动，行内代码同样有底色；引用块与图片也不再溢出。
### 6. 修复：EPUB 电子书一本都打不开（重要）
- 修复了一个底层资源损坏问题：EPUB 阅读器依赖的脚本文件内容被破坏，导致打开任何 EPUB 都停在
- 同时修复了书籍地址缺少格式后缀时，阅读器误判为「目录」而卡住的问题。
### 7. 修复：快速连续调整设置时，后改的值可能被覆盖
- 现在连续拖动滑块、连续切换选项只会在停止操作后保存一次最终结果，不会再出现「刚改的又被改回去」。
### 1. 修复：调完字号、行距、字体、对齐、亮度后可能「白调」
- 此前这几项改完并不会立刻保存，必须再点一下设置面板外面才写入；调完直接刷新或关掉页面就丢了。
### 2. 翻页模式新增页码显示
- 底部信息栏在**翻页模式**下会额外显示「第 x / y 页」，与章节进度并列；滚动模式不显示页码。
### 3. EPUB 阅读器新增「排版」面板
- 阅读器顶部新增「排版」按钮，展开可调 **字号（12–28 连续可调）/ 行距 / 亮度 / 字体**，
- 面板开合是 180 毫秒轻淡入；若系统开启了「减少动态效果」，动画会自动关闭。
### 4. EPUB 阅读器跟随你的主题
- 暗色 / 护眼主题下，EPUB 阅读界面的工具栏与正文区域一起换色，不会出现「暗色里突然一块白底」。
### 5. 阅读页设置面板对 EPUB 同样有效
- 在阅读页设置面板里调字号 / 行距 / 亮度 / 主题，现在会**直接作用到 EPUB 正文**（此前完全没有反应）。
- 面板打开时显示的就是你真实的偏好，不再停在默认值。
### 6. 修复：EPUB 阅读进度不保存（重要）
- EPUB 阅读器的进度上报逻辑此前没有接通：翻页后**进度条不动、进度也不会保存**，
### 1. 分类浏览页更好用（/categories）
- **树形层级看得清了**：分类树的每一层加了左侧引导线，鼠标悬停有高亮反馈。
- **展开状态会被记住**：展开哪几个分类，刷新页面或切换视图后仍然保持。
- **新增「全部展开 / 全部收起」按钮**：想看整棵树一次点开，想收起来一键收起。
### 2. 分类管理页可删除空分类（管理员）
- 分类管理页每行新增「删除空分类」按钮；**只对空分类可点**，其下还有书籍或子分类的行会自动置灰
### 3. 首页新增「标签」筛选
- 书架筛选栏新增「标签」一行：列出常用标签（带数量），**点一下即按该标签筛选，再点一次取消**；
- 标签较多时默认只显示前 12 个，可点「更多」展开全部；库内还没有标签时这一行自动隐藏，不占版面。
### 4. 修复：分类浏览页数量不显示
- 分类浏览页顶部的「分类数 / 藏书数」此前不会显示（脚本读了一个页面上并不存在的元素，
### 5. 修复：部分分类名「看不见的字符」与排序错乱
- 仅处理显示，**分类路径未做任何改动**，原有链接与归类完全正常。
### 1. 新增「整理工具」卡片（书籍管理页）
- **文件名智能整理**—— 扫描出「文件名叫得不规范」的书，给出「原名 → 建议名」对照与整理理由
- **查找分册（上下册 / 第N部 / 第N卷）**—— 把同一套书的上下册、第几部自动聚成一组并排好顺序，可整组合并成一本。
- **查找分章（第N章 / 编号结尾）**—— 同上，针对「书名-1、书名-2」这类分章文件。
### 2. 新增单本书「拆分」
- 书籍列表每行新增「拆分」按钮：可把一个纯文本（txt / md）**按章节标题切**，或**按长度等分切**
- 弹窗里先点「预览方案」，看清将生成哪几卷、每卷多少字、起始是哪一行，再点「确认拆分」。
- 拆分结果是**新增分卷，原书保留不动**；分卷自动继承原书的分类与格式，并直接入库可读。

---

## [2.4] - 2026-09-19

插件系统正式开放：插件不再只是首页上的一个按钮，而是可以带来独立页面、管理入口、阅读页工具
与图形化配置。同时后台书籍管理与阅读页外观都有较大增强。

### 🧩 插件系统

- **三种插件来源**：内置插件、随发行包提供的示例插件，以及**用户自己放入的持久目录**
  （重新部署不会丢失）。插件加载失败不会影响应用运行，会在「插件管理」中标红并显示具体错误。
- **插件可以做到什么**（不再局限于首页一个入口）：
  - **独立页面**：用一个 Blueprint 提供自己的路由、模板与静态资源
  - **首页入口**：注册到首页「扩展功能」面板，支持跳转链接、内置小组件，或由插件端点返回
    HTML 片段实时注入卡片
  - **管理侧栏**：向管理员侧栏注入链接
  - **阅读页工具**：向阅读页工具栏注入按钮
- **图形化设置**：插件只需声明一组配置字段（文本 / 整数 / 小数 / 开关 / 下拉 / 多行文本），
  「插件管理」页会自动渲染成表单，改完即时保存 —— 插件作者不需要写任何前端代码。
  需要更复杂的界面时，插件也可以提供一个自定义设置页。
- **插件管理页**：显示插件来源、作者、导入错误与各项能力标识，并支持「重新扫描插件目录」——
  放入新的插件文件后无需重启即可注册。
- **示例插件**：随包提供一个「书库统计」插件（总量 / 格式分布 / 热门分类 / 最近入库），
  演示上述全部能力；统计维度本身也由图形化设置控制。
- **开发文档**：[插件规范标准](docs/插件规范标准.md)（权威规格）与
  [插件开发指南](docs/插件开发指南.md)（快速上手）随仓库提供。

### 📖 阅读页

- **禅模式**：一键进入沉浸阅读 —— 隐藏导航、页脚、推荐区与工具栏，正文收窄居中；右上角保留
  一个低透明度的退出按钮，也可按 `Esc` 退出，状态本地记忆。快捷键 `Z`。
- **夜间 / 主题快捷键**：`D` 键一键切换主题。
- **亮度调节**：设置面板新增亮度滑块（30~150），只影响正文，不改变系统亮度。
- **章节快捷跳转**：工具栏新增章节下拉，长书跳转更顺手。
- **正文链接处理**：点击正文里的链接不再直接跳走，而是弹出「打开链接 / 复制链接」。
- **背景纹理**：新增格子 / 点阵 / 横线 / 羊皮纸四种纹理（纯样式实现，不加载任何外部图片）。
- **自定义页面颜色**：可用取色器直接指定页面底色。

### 🔍 首页与检索

- **分类面包屑**：从侧栏进入分类后，书架顶部显示当前分类路径，每一级都可点击回退，
  「重置筛选」时自动隐藏。
- **搜索统计更完整**：搜索命中的书除格式分布外，新增**分类分布**；点击某个分类即可把它直接
  加入筛选，实现「搜到一个分类 → 一键收窄」。
- **目录展开记忆**：侧栏目录的展开 / 折叠状态会被记住，刷新后自动恢复。
- **移动端**：后台书籍宽表在窄屏改为横向滚动，不再溢出。

### 🛠 后台书籍管理

- **封面上传与清除**：可自行指定书籍封面（jpg / png / webp / gif，≤5MB）。
- **书籍合并**：可把多本纯文本按顺序合并为一本，流式读写，超大文件也不会占用过多内存；
  源文件默认保留，确认后才删除。
- **自定义分类**：可新建分类（支持指定上级）；删除分类带安全熔断 —— 分类下仍有书籍或子分类
  时拒绝删除，避免误删真实目录数据。
- **失效书籍清理**：可扫描「磁盘文件已丢失但仍在库中」的记录并清理；清理前会再次确认文件确实
  不存在，**绝不删除磁盘上真实存在的文件**。
- **批量操作**：书籍列表支持全选 / 多选，可批量移动分类、增删标签、删除。
- **元数据导出**：可将书籍元数据导出为 CSV（含 BOM，Excel 打开中文不乱码），流式生成，
  不受书库规模影响。
- **书籍信息编辑**：可修改书名 / 作者 / 格式 / 分类 / 简介。
- **导入增强**：支持 ZIP 压缩包批量导入（安装 `rarfile` 后亦支持 `.rar`），自动解压并在导入
  完成后清理临时文件；可依据文件名或压缩包内目录自动归入对应分类。

### 🐛 修复

- **书籍封面一直不显示**：封面地址未走框架的静态资源路由，导致请求 404。已修正，并兼容历史
  数据中保存的绝对路径。
- **搜索统计从未生效**：后端算好了统计数据却漏加进返回结果，前端始终拿到空值，格式分布一直
  没有显示过。已补上。
- **查重去重卡死**：原实现对每个重复组再发起一次数据库查询，在大型书库上会退化成数万次查询。
  已改为单次查询后在内存中分组，秒级返回。
- **分类管理页内存爆掉**：该页原先为每一行都渲染一个包含全部分类的下拉框，选项数以百万计。
  已改为全页共用一个「合并目标」下拉加行筛选输入框。
- **首页侧栏与「常用分类」乱码刷屏**：改为只展示名称正常且书籍数达标的分类。
- **扩展功能卡片错位 / 宽度不齐**：清理了冲突的重复样式规则，卡片改为等宽铺满。
- **页脚样式被当作正文渲染**：页脚样式块缺少标签包裹，浏览器提前结束 `<head>`，导致页面顶部
  出现一大段样式文本和横向滚动条。已修复。

---

## [2.3] - 2026-09-18

本次更新集中在阅读体验、首页布局与管理员能力。

### 📖 阅读页

- **工具栏改为文字按钮**：原先的一排图标全部换成中文文字（上一章 / 下一章 / 目录 / 搜索 /
  全屏 / 设置 / 主题 / 返回），窄屏下横向滚动，不再出现被压缩成半截的按钮。
- **PC 端状态栏常驻**：桌面阅读时不再随滚动隐藏工具栏（移动端保留滚动自动隐藏）。
- **设置面板精简**：「重置并关闭」改为「重置」（只重置、不关闭）；移除冗余的「保存并关闭」
  —— 点击设置面板以外的区域即自动保存。
- **`Ctrl` + 滚轮**：改为只调整正文字号与行距（不再缩放整个页面），松手后自动保存。
- **沉浸模式**：可隐藏工具栏与面板，点击外部或按 `Esc` 退出。
- **章节位置**：状态栏显示「第 N / Y 章」，随滚动与跳转实时更新。
- **触摸手势**：左右滑动切换章节、双指捏合调节字号，并做了横屏适配。
- **排版增强**：行距、段间距、首行缩进等排版项可控。
- **主题跟随系统**：可让阅读页主题跟随系统深色模式。

### 🏠 首页

- **继续阅读更紧凑**：由整宽卡片改为内联条，长书名省略号收尾，不再撑高。
- **搜索防抖**：搜索框改为停顿后才发起请求，不再每按一键就刷新一次列表。
- **筛选栏扁平化**：「全部」按钮改为与其它筛选按钮一致的尺寸，视觉主次更清楚。
- **侧栏吸顶**：分类导航在滚动时保持可见，内部独立滚动。
- **书架排序**：支持按书名 / 作者 / 时间 / 阅读进度排序，可升可降。
- **格式徽标**：书籍卡片上的格式由纯文本改为彩色徽标，格式一眼可辨。
- **移动端**：侧栏在窄屏改为可折叠，不再固定占用高度。

### 🧩 插件与扩展

- **插件框架落地**：首页上那些零散的入口由一套插件机制统一承载，后续新功能以「注册一个插件」
  的方式接入，不再逐处硬编码。
- **首页「扩展功能」面板**：承载插件入口，内置「最近阅读」（带进度条）、「随便看看」、
  「相关推荐」。
- **插件管理页**：可查看与开关插件，非管理员只读。

### 👤 用户与权限

- **子管理员授权**：最高管理员可授权**最多 3 名**子管理员，并按能力分别授予
  「导入 / 导出 / 书籍管理」权限。
- **阅读状态**：书籍支持未读 / 在读 / 已读。
- **书籍标签**：可给书籍打标签，标签参与搜索。
- **分类页增强**：分类内搜索与分页。

### 🐛 修复

- **登录后所有页面不可用（严重）**：新增的用户字段未登记到数据库结构同步逻辑，导致任何用户
  查询都报错。已修复，并补上通用的「按模型声明自动补齐缺失列」兜底，避免同类问题复发。
- **书架整页空白（严重）**：排序代码误用了一个需要参数却被无参调用的排序函数，导致书架接口
  无论传什么参数都报错。已修复。

---

## [2.2] - 2026-09-16

本次为**稳定性与性能修复版本**。核心是修复了一个会导致扫描后书籍全部消失的严重缺陷，
并针对大规模书库做了一轮性能优化。

### 🔴 严重缺陷修复

- **修复扫描后书籍全部消失**
  - **根因**：书库目录中存在循环符号链接，宿主机上该链接可以正常解析，但在容器内挂载点的
    视角下链接断裂；同时孤儿分类清理逻辑存在路径重复拼接问题。两者叠加导致全部分类被误判
    为不存在，连带删除了库中的全部书籍。
  - **修复**：
    - 根分类不再重复拼接 `books/` 前缀
    - 孤儿清理增加**熔断保护**：待删除分类超过总量 50% 时中止操作，宁可保留脏数据也不批量删书
    - 扫描时跳过符号链接目录（此前会导致无限递归直至栈溢出）
  - **验证**：按容器视角复算，误杀率由 **100% 降至 0%**；全量扫描后待清理分类数为 0，
    二次增量扫描数量不再回落。

- **修复被禁用账号仍可登录**
  登录逻辑只校验密码、从不检查账号启用状态，导致后台「禁用用户」形同虚设。现已增加账号状态
  校验，停用账号会被拒绝登录并给出明确提示。

- **修复阅读设置无法保存 / 主题刷新后不恢复**
  设置面板移除「边距」控件后，前端代码仍在读取该元素，导致加载与保存设置的函数抛出异常：
  主题保存后刷新页面不生效，字号 / 行距实际上从未写入。已补充空值判断并清理死控件。

- **取消默认弱口令**
  首次初始化不再使用固定的弱口令账号。启动时会扫描常见弱口令，命中即自动停用（会跳过隐藏
  账号与最后一位活跃管理员，避免把自己锁在门外）。

- **移除内置固定管理员口令（发布安全）**
  此前存在写死在源码中的隐藏管理员账号。镜像一旦公开发布，所有人都能在源码里看到这串凭据，
  从而登录**任意一套**使用该镜像的部署。现改为：
  - 首次启动时随机生成口令，并**只向容器日志打印一次**
  - 需要固定口令时通过环境变量 `SR_ADMIN_USERNAME` / `SR_ADMIN_PASSWORD` 显式指定
  - 口令遗失后可通过 `SR_ADMIN_PASSWORD` 重启容器重置
  - 同时移除了启动脚本中创建固定默认账号的逻辑

- **移除固定 `SECRET_KEY` 默认值（发布安全）**
  原代码在环境变量缺失时回落到写在源码里的常量。会话 cookie 用它签名，密钥一旦公开，攻击者
  可以伪造任意用户的会话直接取得管理员身份。现改为首次运行时随机生成并持久化到
  `instance/secret.key`（权限 600），既保证重启后登录状态不失效，也不存在通用密钥。

- **从版本库中移除敏感文件**
  历史提交中误入了会话 Cookie 文件与包含真实账号数据的 SQLite 数据库，现已全部取消跟踪，
  并补充忽略规则禁止此类文件再次入库。

### ⚡ 性能优化

- **书库首页加载 7.5~11.5s → 0.06s（约 180 倍）**
  首页此前会把全部分类递归渲染成完整树形结构。现已改为仅渲染一级分类的扁平列表，下钻导航
  交由已有的分类页完成。

- **数据库索引 0 → 9 个**
  新增书名 / 格式 / 分类 / 最后阅读 / 首字母、阅读进度等索引。此前全库查询均为全表扫描。

- **扫描改为后台异步执行**
  此前通过 HTTP 触发扫描需同步等待约 20 分钟，必然超时。现实现：
  - Web 服务改用 `waitress`（多线程）替代 Flask 开发服务器
  - 启动扫描的请求改为在后台线程执行并**立即返回**，重复提交会被拒绝
  - 新增进度查询接口，扫描期间站点保持正常响应

- **扫描性能**
  - 批量提交：改为每 500 条提交一次，避免十万次级的事务提交
  - 分类计数由递归查询改为一次聚合查询
  - 修正增量扫描的缓存键不一致问题，省去数万次重复字符串拼接
  - 增量扫描耗时 109s → 42s

### ✨ 新增功能

- **中文书名按拼音首字母归类**
  新增首字母列及索引，入库时计算并持久化（而非查询时实时计算）。分词支持跳过 `《` `【` `[`
  等前缀符号查找首个有效字符，并对生僻字、日文假名、全角字符做归一化与合法性校验。
  「其他」分类占比由约 69% 降至 0.3% 以内。

- **书籍详情页**
  此前不存在该页面。现提供封面、开始 / 继续阅读、阅读进度、文件格式与大小、分类面包屑、
  修改时间及相关推荐。

- **书签功能**
  新增书签表与配套接口，阅读页提供书签面板。添加书签时会自动摘取当前位置附近约 40 字作为
  摘录，便于回看时快速定位上下文。

- **阅读器增强**
  - 点触翻页：左侧上翻、右侧下翻、中间区域切换工具栏显隐（移动端可用）
  - 阅读进度百分比显示与进度条拖拽跳转
  - 章节目录面板（自动识别常见章节格式）与点击跳转
  - 上一章 / 下一章快捷切换
  - 页内正文搜索并高亮命中
  - 全屏模式，工具栏自动隐藏

- **容器健康检查**
  镜像内置 `HEALTHCHECK`，配合 `--restart unless-stopped` 可在服务异常时自动重启。

### 🐛 其他修复

- 首页字母统计一直无数据，原因是统计接口查询的表名有误
- 删除无引用的死代码文件
- 扫描接口恢复 GET 支持（此前返回 405）
- 删除书籍前先清理关联的阅读进度，避免留下悬空数据
- 书名 / 文件名字段长度上限由 200 提升到 512，修复长文件名被截断
- 封面保存改用绝对路径，避免切换工作目录后保存失败
- 清理重复定义的同名函数，并将全站原生弹窗统一为应用内提示

### ⚙️ 升级提示

本版本涉及 `Dockerfile` 与启动脚本的变更，**需重新构建（或拉取新）镜像**，不能仅重启容器。
升级后建议触发一次全量扫描以重建分类与书名首字母索引。

---

## [2.1] - 2026-08-30

- 首个公开发布版本
- 支持 TXT / EPUB / PDF 等多种格式阅读
- 目录树分类、分页加载、多格式筛选
- 自动检测 GBK / GB18030 / BIG5 / UTF-8 编码
- 用户注册登录、邀请码、管理员后台
- 阅读进度自动保存，首页继续阅读入口
